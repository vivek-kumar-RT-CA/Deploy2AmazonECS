version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "my-task-definition"
        LoadBalancerInfo:
          ContainerName: "my-web-server"
          ContainerPort: 80
